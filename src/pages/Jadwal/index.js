import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const Jadwal = ({}) => (
  <View style={styles.container}>
    <Text>I'm Jadwal</Text>
  </View>
);

export default Jadwal;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
